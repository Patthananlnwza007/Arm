#include <stdio.h>

int main() {
    int num = 2; // เริ่มต้นที่เลขคู่ตัวแรกคือ 2

    do {
        printf("%d ", num);
        num += 2; // เพิ่มทีละ 2 เพื่อให้เป็นเลขคู่ถัดไป (2, 4, 6, ...)
    } while (num <= 20); // ทำซ้ำตราบใดที่ num ยังไม่เกิน 20

    return 0;
}
